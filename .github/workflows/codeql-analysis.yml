jobs:
  analyze:
    # …
    strategy:
      fail-fast: false
      matrix:
        language: [ "javascript" ]
    steps:
      # …
      - name: Initialise CodeQL
        uses: github/codeql-action/init@v2
        with:
          config-file: .github/codeql/codeql-config.yml
